# Medical Talent Training Alliance Mobile

医学人才培养联盟官方网站-移动端

## 📋 项目简介

医学人才培养联盟（MTT Alliance）官方网站，基于 Egg.js 框架构建的企业级 Web 应用。项目提供多语言支持，包含新闻资讯、活动管理、视频中心、人才发展、数据资料等核心功能模块。

## 🛠 技术栈

### 后端框架
- **Egg.js** ^2.2.1 - 企业级 Node.js 框架
- **EJS** - 模板引擎
- **egg-i18n** - 国际化支持

### 前端技术
- **Vue.js** - 渐进式 JavaScript 框架
- **Axios** - HTTP 客户端
- **Less** - CSS 预处理器

### 构建工具
- **Gulp** - 自动化构建工具
- **Webpack** - 模块打包工具
- **Babel** - JavaScript 编译器

### 开发工具
- **ESLint** - 代码检查工具
- **BrowserSync** - 浏览器同步工具

## ✨ 功能特性

- 🌍 **多语言支持** - 支持中文、英文、法文等多种语言
- 📰 **新闻资讯** - 新闻列表、详情、热点新闻
- 🎬 **视频中心** - 视频列表、视频详情
- 🎯 **活动管理** - 活动列表、活动详情、年会详情
- 👥 **人才发展** - 国际项目、国内项目、培训详情
- 📊 **数据资料** - 资料中心、资料详情、相关政策
- 🔍 **搜索功能** - 全站内容搜索
- 👤 **用户系统** - 登录、成员管理

## 🚀 快速开始

### 环境要求

- Node.js >= 8.9.0
- npm 或 yarn

### 安装依赖

```bash
# 安装主项目依赖
$ npm install

# 安装子项目依赖（如果存在）
$ cd user
$ npm install
$ cd ..
```

### 开发模式

```bash
# 启动开发服务器（包含 mock 数据）
$ npm run mock

# 或直接启动开发服务器
$ npm run dev

# 访问 http://localhost:7001/
```

### 生产构建

```bash
# 构建生产版本
$ npm run build

# 启动生产服务器
$ npm start

# 停止生产服务器
$ npm stop
```

## 📁 项目结构

```
mtt_alliance_web/
├── app/
│   ├── controller/          # 控制器
│   │   ├── home.js         # 首页控制器
│   │   └── post.js         # 接口控制器
│   ├── service/            # 业务逻辑层
│   │   ├── api.js          # API 服务
│   │   ├── article.js      # 文章服务
│   │   └── user.js         # 用户服务
│   ├── router/             # 路由配置
│   │   ├── page.js         # 页面路由
│   │   └── post.js         # 接口路由
│   ├── view/               # 视图模板
│   │   ├── component/      # 公共组件
│   │   ├── news/           # 新闻相关页面
│   │   ├── video/          # 视频相关页面
│   │   └── ...
│   ├── public/             # 静态资源
│   │   └── pro/
│   │       ├── css/        # 样式文件
│   │       ├── js/         # JavaScript 文件
│   │       ├── img/        # 图片资源
│   │       └── less/       # Less 源文件
│   ├── middleware/         # 中间件
│   └── mock/               # Mock 数据
├── config/                 # 配置文件
│   ├── config.default.js   # 默认配置
│   ├── config.local.js     # 本地配置
│   ├── config.prod.js      # 生产配置
│   └── config.test.js      # 测试配置
├── test/                   # 测试文件
├── gulpfile.js            # Gulp 配置文件
└── package.json           # 项目配置
```

## 📜 可用脚本

### 开发相关
- `npm run dev` - 启动开发服务器（包含文件监听）
- `npm run local` - 启动本地开发服务器（端口 3000）
- `npm run mock` - 启动开发服务器（包含 mock 数据）
- `npm run debug` - 启动调试模式

### 构建相关
- `npm run build` - 构建生产版本
- `npm run script` - 构建测试版本
- `npm run clean` - 清理构建文件

### 测试相关
- `npm test` - 启动测试服务器（端口 8500）
- `npm run test-local` - 运行单元测试
- `npm run cov` - 生成测试覆盖率报告
- `npm run tstop` - 停止测试服务器

### 代码质量
- `npm run lint` - 代码风格检查
- `npm run ci` - 运行 lint 和测试

### 部署相关
- `npm start` - 启动生产服务器（端口 8000）
- `npm stop` - 停止生产服务器

### 其他
- `npm run autod` - 自动检测依赖更新

## 🌍 多语言支持

项目支持以下语言：
- 中文简体 (zh-CN) - 默认语言
- 英文 (en-US)
- 法文 (fra-FRA)

语言切换通过 URL 参数 `locale` 或 Cookie 实现。

## 🔧 配置说明

### 环境变量

- `NODE_ENV` - 环境类型（development/test/production）
- `EGG_SERVER_ENV` - Egg 服务器环境（local/test/prod）

### 端口配置

- 开发环境：3000（local）或 7001（dev）
- 测试环境：8500
- 生产环境：8000

## 📝 开发指南

### 添加新页面

1. 在 `app/view/` 目录下创建对应的 HTML 模板
2. 在 `app/controller/` 中添加对应的控制器方法
3. 在 `app/router/page.js` 中添加路由配置

### 添加新接口

1. 在 `app/controller/post.js` 中添加接口处理逻辑
2. 在 `app/router/post.js` 中添加路由配置
3. 如需调用外部 API，在 `app/service/api.js` 中添加方法

### 样式开发

- Less 源文件位于 `app/public/pro/less/`
- 使用 Gulp 自动编译 Less 到 CSS
- 生产环境会自动压缩和优化 CSS

## 🚢 部署

### 生产环境部署

```bash
# 1. 构建项目
$ npm run build

# 2. 启动服务
$ npm start

# 3. 停止服务
$ npm stop
```

### 测试环境部署

```bash
# 启动测试服务器
$ npm test

# 停止测试服务器
$ npm run tstop
```

## 📚 相关文档

- [Egg.js 官方文档](https://eggjs.org)
- [Vue.js 官方文档](https://vuejs.org)
- [Gulp 官方文档](https://gulpjs.com)

## ☕ 请作者喝杯奶茶

如果这个项目对你有帮助，欢迎请作者喝杯奶茶 🥤

![收款二维码](https://raw.githubusercontent.com/RiverGodo/gallery/main/pay.png)

## 📄 License

MIT